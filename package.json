{
  "name": "masterytv",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "safety": "node scripts/coach-lab/safety-battery.mjs",
    "isolation": "node scripts/coach-lab/isolation-check.mjs",
    "snapshot:prompts": "deno run --node-modules-dir=none --allow-net --allow-env --allow-read --allow-write scripts/coach-lab/prompt-snapshot.ts",
    "check:resolve": "deno run --node-modules-dir=none --allow-net --allow-env --allow-read scripts/coach-lab/resolve-program-check.ts",
    "check:metadata": "node scripts/check-brand-metadata.mjs",
    "check:colors": "node scripts/check-brand-colors.mjs",
    "check:brand-tokens": "node scripts/check-brand-tokens.mjs",
    "check:brand-terms": "node scripts/check-banned-brand-terms.mjs",
    "check:copy-tells": "node scripts/check-copy-tells.mjs",
    "check:ternaries": "node scripts/check-brand-ternaries.mjs",
    "check:tenancy": "node scripts/check-tenancy.mjs",
    "gate": "npm run typecheck && npm run test && npm run safety && npm run isolation && npm run check:metadata && npm run check:colors && npm run check:brand-tokens && npm run check:brand-terms && npm run check:copy-tells && npm run check:ternaries && npm run check:tenancy && npm run check:resolve && npm run snapshot:prompts"
  },
  "dependencies": {
    "@react-pdf/renderer": "^4.0.0",
    "@stripe/stripe-js": "^5.0.0",
    "@supabase/ssr": "^0.10.0",
    "@supabase/supabase-js": "^2.101.0",
    "@tailwindcss/postcss": "^4.2.2",
    "@xstate/react": "^6.1.0",
    "framer-motion": "^12.38.0",
    "lucide-react": "^1.7.0",
    "next": "^15.1.0",
    "openai": "^4.0.0",
    "postcss": "^8.5.8",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-markdown": "^10.1.0",
    "stripe": "^17.0.0",
    "tailwindcss": "^4.2.2",
    "xstate": "^5.30.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.6"
  }
}
